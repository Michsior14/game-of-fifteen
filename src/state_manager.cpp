#include "state_manager.h"

StateManager::StateManager(MoveStack* moveStack, Blocks* blocks): _blocks(blocks), _moveStack(moveStack) {}

void StateManager::saveGame(std::string path){

}

void StateManager::loadGame(){
}
